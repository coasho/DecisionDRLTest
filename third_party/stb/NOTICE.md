stb_image.h v2.30 (https://github.com/nothings/stb) by Sean Barrett - public domain / MIT
(dual licence, see the end of the header). Used by io::TerrainTiles to decode PNG elevation tiles.

stb_image_write.h v1.16 (same origin and licence). Used by fsim_vision to write camera images as PNG.
